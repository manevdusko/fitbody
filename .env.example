# =============================================================================
# WordPress API Configuration
# =============================================================================
# These URLs point to your headless WordPress backend at api.fitbody.mk
# The frontend fetches all data via REST API from this domain

# WordPress REST API base URL
WORDPRESS_API_URL=https://api.fitbody.mk/wp-json/wp/v2

# WooCommerce REST API URL (via custom proxy)
WOOCOMMERCE_API_URL=https://api.fitbody.mk/wp-json/wc/v3

# API domain (used for image optimization and CORS)
API_DOMAIN=api.fitbody.mk

# =============================================================================
# Next.js Public Configuration
# =============================================================================
# These variables are exposed to the browser (prefixed with NEXT_PUBLIC_)

# Your site's public URL (used for SEO, social sharing, etc.)
NEXT_PUBLIC_SITE_URL=https://fitbody.mk

# Application name (displayed in browser title, meta tags, etc.)
NEXT_PUBLIC_APP_NAME=FitBody.mk

# API domain (public, used for client-side requests)
NEXT_PUBLIC_API_DOMAIN=api.fitbody.mk

# =============================================================================
# Deployment Configuration (Optional)
# =============================================================================

# Base path for subdirectory deployment (e.g., /repo-name for GitHub Pages)
# Uncomment and set if deploying to https://username.github.io/repo-name/
# BASE_PATH=/repo-name

# Deployment environment (development, staging, production)
# NODE_ENV=production

# =============================================================================
# Development Configuration (Optional)
# =============================================================================
# Uncomment these for local WordPress development

# Local WordPress API
# WORDPRESS_API_URL=http://localhost/wordpress/wp-json/wp/v2
# WOOCOMMERCE_API_URL=http://localhost/wordpress/wp-json/wc/v3

# Local site URL
# NEXT_PUBLIC_SITE_URL=http://localhost:3000

# =============================================================================
# Notes
# =============================================================================
# 1. Never commit .env.local or .env.production files to version control
# 2. This .env.example file should be committed to show required variables
# 3. Copy this file to .env.local for local development
# 4. GitHub Actions uses environment variables set in the workflow file
# 5. All NEXT_PUBLIC_* variables are exposed to the browser
# 6. Non-prefixed variables are only available server-side (build time)
# =============================================================================